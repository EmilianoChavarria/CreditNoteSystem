// Copy this file to environment.ts and fill in your local values.
// environment.ts is gitignored — never commit it.
export const environment = {
  production: false,
  apiBaseUrl: 'http://<LOCAL_IP>:8000/api',
  socketHost: '<LOCAL_IP>',
  socketPort: 8080,
};
